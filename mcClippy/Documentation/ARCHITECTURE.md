# mcClippy Architecture

mcClippy is a local-first macOS menu bar clipboard history app. The app intentionally keeps clipboard content on-device and treats copied data as sensitive by default.

## Runtime Components

| Component | File | Responsibility |
| --- | --- | --- |
| App entry and menu bar | `mcClippyApp.swift` | SwiftUI app scene, menu bar commands, Settings, panel controller, pasteboard monitor, serializer, and global hotkey. |
| History panel | `ContentView.swift` | Search, filters, keyboard selection, paste actions, reveal controls, pin/delete actions, and row rendering. |
| Clipboard item model | `Item.swift` | SwiftData model for persisted clipboard entries. |
| Encryption | `Encryption.swift` | ChaChaPoly sealing/opening with a Keychain-stored symmetric key. |
| Auto-paste | `AutoPaste.swift` | Accessibility trust checks and Cmd+V CGEvent posting. |
| App exclusions | `AppExclusions.swift` | UserDefaults-backed bundle-ID denylist. |
| Shortcut settings | `Shortcuts.swift` | Shortcut model, storage, recorder UI, and hotkey reload trigger. |
| Product settings | `ProductSettings.swift` | History limits and launch-at-login settings. |
| Onboarding | `Onboarding.swift` | First-run explanation of permissions and privacy behavior. |

## Data Flow

1. `PasteboardMonitor` polls `NSPasteboard.general.changeCount`.
2. If monitoring is active and the frontmost app is not excluded, `PasteboardSerializer.snapshot(from:)` extracts supported content.
3. Concealed, transient, autogenerated, known password-manager, and Universal Clipboard marker types force the snapshot to be marked sensitive when supported content is present.
4. The raw content is hashed for duplicate detection.
5. The raw data blob is encrypted with `EncryptionService`.
6. Sensitive previews are stored as a placeholder; non-sensitive previews are stored as trimmed display text.
7. SwiftData persists the `Item`.
8. Reboot-scoped retention clears regular and/or pinned items once per Mac boot when their policies are set to the default `Until reboot`.
9. The panel queries `Item` records and decrypts only when restore, thumbnail display, or explicit sensitive reveal needs it.

## Paste Flow

1. User presses the configured global shortcut.
2. `GlobalShortcutManager` posts `.pasteHistoryShortcutPressed`.
3. `PasteHistoryPanelController` records the previous frontmost app before activating mcClippy.
4. User presses Return or clicks a row.
5. `PasteboardSerializer.restore` decrypts and writes the selected content to `NSPasteboard.general`.
6. The panel closes.
7. If enabled and Accessibility is trusted, `AutoPaster` activates the previous app and posts Cmd+V.
8. If Accessibility is not trusted, mcClippy still returns focus to the previous app with the selected item on the clipboard.

## Privacy Constraints

- `plainTextPreview` must never store actual sensitive text.
- `dataBlob` should be encrypted for all newly captured items.
- If the Keychain key cannot be created or read, capture fails closed.
- Password-manager and concealed pasteboards are masked by default and remain pasteable when captured.
- App exclusions are best-effort because they depend on the frontmost app at polling time.

## Known Architecture Debt

- `mcClippyApp.swift` owns too many components. Split monitor, serializer, panel controller, settings views, and hotkey manager into separate files before release.
- Source app attribution is best-effort.
- Rich text is currently restored as text. Preserve HTML/RTF types if rich text fidelity becomes a release requirement.
- The panel UI decrypts image blobs for thumbnails. This is necessary for preview, but image memory handling should remain scoped to rendering.
